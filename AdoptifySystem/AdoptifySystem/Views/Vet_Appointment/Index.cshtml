@model IEnumerable<AdoptifySystem.Vet_Appointment>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Veterinarian.Vet_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Veterinarian.Vet_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Vet_Appointment_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Vet_Appointment_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Vet_Appointment_ID })
        </td>
    </tr>
}

</table>
